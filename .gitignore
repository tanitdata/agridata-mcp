# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Virtual environment
.venv/
venv/

# Environment variables
.env

# Testing
.pytest_cache/

# IDE
.vscode/
.idea/

# Regeneratable data (run scripts/ to rebuild)
audit_full.json
vocabulary_raw.json
value_catalog.json

# Raw scrape (local-only). The snapshot architecture that used these
# files lives on tag v3.1.3; see CLAUDE.md "Snapshot fallback" for the
# recovery procedure if the portal goes down again.
agridata_TN_Scraped_*/
snapshot/

# Research (local only, not for public repo)
RESEARCH.md
research/

# Claude Code development context (local only, not for public repo)
CLAUDE.md

# Ad-hoc scripts and assets (local only)
list_stations.py
test_domain.py
trust-policy-final.json
tanitdata_logo_v2.png
_phase*
memory/
